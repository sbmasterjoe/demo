- host: linux_demo
   name: deploy
   gather_facts: false
   vars_prompt:
	-name: "esxi_password"
	 prompt: "exsi pass"
	-name: "vcenterpass"
	 prompt: "vc pass"
	-name: "vcenterlicens"
	 prompt: "vc licens"
	-name: "vsphere licens"
	 prompt: "vsp licens"
  vars_files: vable.yml
 tasks:
   - name: setting fact
      set_fact:
	#上面参数:"{{ 相关var}}"


- import_playbook:
- import_playbook:
- import_playbook:
- import_playbook:
